@page "/employeeForm"
@using Turntablio.RoleService.Data
@using Turntablio.RoleService.Pages.ViewModel
@using Turntablio.RoleService.Data.Model
@using Turntablio.RoleService.Pages.Employee

@inject Turntablio.RoleService.Data.Services.EmployeeService  employeeService
@inject NavigationManager navigationManager

<h2>Add New Employee</h2>
<hr />

<div class="row">
    <div class="col-md-8">
        <EditForm Model="@objEmp" OnValidSubmit="@InsertEmployee">
            <DataAnnotationsValidator />
            <ValidationSummary Model="@objEmp" />

                <div class="form-group">
                    <label for="employeefname" class="control-label">First Name</label>
                    <InputText id="employeefname" class="form-control" @bind-Value="objEmp.EmployeeFirstName" />
                    <ValidationMessage For="@(() => objEmp.EmployeeFirstName)" />
                </div>
                <div class="form-group">
                    <label for="employeelname" class="control-label">Last Name</label>
                    <InputText id="employeefname" class="form-control" @bind-Value="objEmp.EmployeeLastName" />
                    <ValidationMessage For="@(() => objEmp.EmployeeLastName)" />
                </div>
                <div class="form-group">
                    <label asp-for="employeeemail" class="control-label">Email</label>
                    <InputText id="employeeemail" class="form-control" @bind-Value="objEmp.EmployeeEmail" />
                    <ValidationMessage For="@(() => objEmp.EmployeeEmail)" />
                </div>
                <div class="form-group">
                    <label asp-for="employeeaddress" class="control-label">Address</label>
                    <InputText id="employeeaddress" class="form-control" @bind-Value="objEmp.EmployeeAddress" />
                    <ValidationMessage For="@(() => objEmp.EmployeeAddress)" />
                </div>
                <br />

                <div class="form-group">
                    <button type="submit" class="btn btn-primary mr-4">Save</button>
                    <input type="button" class="btn btn-danger" @onclick="@Cancel" value="Cancel">
                </div>

        </EditForm>
    </div>
</div>


@code {

    Employee objEmp = new Employee();

    protected void InsertEmployee()
    {
        employeeService.AddEmployee(objEmp);
        navigationManager.NavigateTo("employees");
    }

    protected void Cancel()
    {
        navigationManager.NavigateTo("employees");
    }

}
